name: audio_converter_example
description: Example app for audio_converter_native package
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audio_converter_native:
    path: ../
  path_provider: ^2.1.1
  permission_handler: ^11.0.1
  record: ^5.0.4
  audioplayers: ^5.2.1
  share_plus: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
